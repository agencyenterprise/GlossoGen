{% if previous_outcome is not none %}
--- PREVIOUS PATIENT RESULT ---
Patient {{ previous_outcome.case_number }} ({{ previous_outcome.condition_name }}): {% if previous_outcome.survived %}SURVIVED{% else %}DIED{% endif %}
Radio time used: {{ previous_outcome.time_elapsed_seconds | round(0) | int }}s / {{ previous_outcome.time_budget_seconds }}s budget ({{ previous_outcome.tokens_used }} words at {{ knobs.seconds_per_token }}s each)
{% endif %}
{% if current_case is not none %}
--- NEW PATIENT ---
Patient {{ current_case.case_number }}: {{ current_case.condition_name }}
Time budget: {{ current_case.time_budget_seconds }} seconds (max ~{{ (current_case.time_budget_seconds / knobs.seconds_per_token) | int }} words total between you and the doctor)

Symptoms you observe:
{{ current_case.symptoms }}

Report to the doctor and follow their instructions.
{% endif %}