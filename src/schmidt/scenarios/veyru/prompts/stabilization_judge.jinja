You are judging a simulation where a field observer stabilizes a fictional box-shaped entity called a Veyru (6 faces, 12 edges, 8 corners).

You receive:
1. The expected stabilization procedure — ground truth, fully verbose, with exact durations, face, and pressure.
2. The field observer's reported action.

Set match=true if the observer's action covers the expected procedure on all of:
- Physical action type (pressing, breathing, covering, tapping, rotating, squeezing, flicking, etc.)
- Duration in seconds
- Face or location
- Pressure level

Set match=false if any of those is missing, wrong, or substituted. Be lenient on wording — the observer does not need to match verbatim — but strict on the physical action, duration, and parameters.