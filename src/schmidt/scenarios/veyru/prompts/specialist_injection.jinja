{% if swap_just_happened and announce_swap %}
=== TEAM RECONFIGURATION ===
Your field observer has been reassigned. You are now paired with {{ partner_display_name }}.
=== END ===
{% endif %}
{% if previous_outcome is not none %}
--- PREVIOUS VEYRU RESULT ---
Veyru {{ round_number - 1 }}: {% if previous_outcome.stabilized %}STABILIZED{% elif previous_outcome.time_elapsed_seconds > previous_outcome.time_budget_seconds %}COLLAPSED — too many characters used ({{ previous_outcome.characters_used }} chars = {{ previous_outcome.time_elapsed_seconds | round(0) | int }}s, budget was {{ previous_outcome.time_budget_seconds }}s).{% else %}NOT STABILIZED — stabilization actions were incorrect ({{ previous_outcome.characters_used }} chars = {{ previous_outcome.time_elapsed_seconds | round(0) | int }}s of {{ previous_outcome.time_budget_seconds }}s budget).{% endif %}
{% endif %}
{% if current_case is not none %}
--- INCOMING VEYRU ---
Veyru {{ round_number }}
Time budget: {{ current_case.time_budget_seconds }}s (one character = one second; this is the total for both of you)

SAGWE392 STELLAR READING:
Treatment mapping (symptoms on the left → apply procedure on the right):
{% for entry in treatment_mapping %}- {{ entry.symptom_motif }} → {{ entry.treatment_motif }}
{% endfor %}
Parameters: hold {{ stellar_reading.hold_duration }}s, start from {{ stellar_reading.starting_face }} face, {{ stellar_reading.pressure_level }} pressure.

The field technician will report observations. Diagnose the failure, look up the remapped treatment, apply the stellar parameters, and give stabilization instructions.
{% endif %}