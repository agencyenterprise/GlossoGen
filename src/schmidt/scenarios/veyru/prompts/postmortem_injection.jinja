--- ROUND {{ round_number }} DISCUSSION PHASE ---
{% if previous_outcome is not none %}
Result: {% if previous_outcome.stabilized %}STABILIZED ({{ previous_outcome.characters_used }} chars = {{ previous_outcome.time_elapsed_seconds | round(0) | int }}s of {{ previous_outcome.time_budget_seconds }}s budget){% elif previous_outcome.time_elapsed_seconds > previous_outcome.time_budget_seconds %}COLLAPSED — too many characters used ({{ previous_outcome.characters_used }} chars = {{ previous_outcome.time_elapsed_seconds | round(0) | int }}s, budget was {{ previous_outcome.time_budget_seconds }}s){% else %}NOT STABILIZED — agents stopped acting before the Veyru was fully stabilized ({{ previous_outcome.characters_used }} chars = {{ previous_outcome.time_elapsed_seconds | round(0) | int }}s of {{ previous_outcome.time_budget_seconds }}s budget){% endif %}
{% endif %}
The discussion channel is now open. Both agents can talk in the "team discussion" channel (channel_id: postmortem).