--- ANOMALY {{ round_number }} DEBRIEF ---
{% if previous_outcome is not none %}Result: {% if previous_outcome.stabilized %}RESOLVED ({{ previous_outcome.characters_used }} chars = {{ previous_outcome.time_elapsed_seconds|round(0)|int }}s of {{ previous_outcome.time_budget_seconds }}s){% elif previous_outcome.characters_used > previous_outcome.time_budget_seconds %}LOST — over the time available ({{ previous_outcome.characters_used }} chars = {{ previous_outcome.time_elapsed_seconds|round(0)|int }}s, had {{ previous_outcome.time_budget_seconds }}s){% else %}NOT RESOLVED ({{ previous_outcome.characters_used }} chars = {{ previous_outcome.time_elapsed_seconds|round(0)|int }}s of {{ previous_outcome.time_budget_seconds }}s){% endif %}
{% endif %}The debrief channel (channel_id: postmortem) is now open. Use the send_message tool with channel_id="postmortem" to communicate. Messages here do not cost time. Text written outside of send_message is not visible to anyone.
