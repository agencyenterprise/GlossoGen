--- ROUND {{ round_number }} DISCUSSION PHASE ---
{% if previous_outcome is not none %}
Result: {% if previous_outcome.recovered %}ROBOT RECOVERED ({{ previous_outcome.characters_used }} chars = {{ previous_outcome.time_elapsed_seconds | round(0) | int }}s of {{ previous_outcome.time_budget_seconds }}s budget){% elif previous_outcome.budget_exceeded %}NOT RECOVERED — communication budget exceeded ({{ previous_outcome.characters_used }} chars = {{ previous_outcome.time_elapsed_seconds | round(0) | int }}s, budget was {{ previous_outcome.time_budget_seconds }}s){% elif previous_outcome.judge_passed %}NOT RECOVERED — recovery action correct but the budget was already exhausted{% else %}NOT RECOVERED — recovery action rejected: {{ previous_outcome.judge_explanation }}{% endif %}
{% endif %}
The "team discussion" channel (channel_id: postmortem) is now open. Use the send_message tool with channel_id="postmortem" to communicate with the other two agents. Messages here do not cost time. Remember: text written outside of send_message is NOT visible to anyone.
