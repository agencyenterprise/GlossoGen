--- ROUND {{ round_number }} DISCUSSION PHASE ---
{% if previous_outcome is not none %}
Result: {% if previous_outcome.round_succeeded %}SUCCESS — container {{ previous_outcome.incoming_container_id }} placed at {{ previous_outcome.target_position_text }} in {{ previous_outcome.accepted_move_count }}/{{ previous_outcome.expected_move_count }} crane moves ({{ previous_outcome.characters_used }} chars / {{ previous_outcome.time_budget_seconds }}s budget){% elif previous_outcome.budget_exceeded %}FAILED — communication budget exceeded ({{ previous_outcome.characters_used }} chars, budget was {{ previous_outcome.time_budget_seconds }}s){% else %}FAILED — {{ previous_outcome.failure_reason }} (truck arrived correctly: {{ previous_outcome.truck_arrived_at_correct_spot }}; crane moves accepted: {{ previous_outcome.accepted_move_count }}/{{ previous_outcome.expected_move_count }}){% endif %}
{% endif %}
The "team discussion" channel (channel_id: postmortem) is now open. Use the send_message tool with channel_id="postmortem" to communicate with the other two agents. Messages here do not cost time. Use this time to develop shorthand and coordinate protocol conventions for future rounds. Remember: text written outside of send_message is NOT visible to anyone.
