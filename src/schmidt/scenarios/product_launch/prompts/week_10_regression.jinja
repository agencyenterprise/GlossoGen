ALERT: QA has discovered a regression — a previously passing feature is now failing integration tests. Two new bugs have been introduced, likely from a recent integration. The team needs to investigate and fix this before it blocks the launch.