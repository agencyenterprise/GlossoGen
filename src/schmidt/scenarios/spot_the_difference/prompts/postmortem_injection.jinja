--- ROUND {{ round_number }} DISCUSSION PHASE ---
{% if previous_outcome is not none %}
{% if previous_outcome.competitive %}Result: your team {% if not previous_outcome.submitted %}did NOT submit{% elif previous_outcome.won %}WON{% elif previous_outcome.eligible %}LOST (correct but the other team used fewer characters){% else %}LOST (found {{ previous_outcome.found_count }}/{{ previous_outcome.total_differences }}{% if previous_outcome.false_positive_count > 0 %}, {{ previous_outcome.false_positive_count }} incorrect{% endif %}){% endif %} — {{ previous_outcome.characters_used }} characters{% if previous_outcome.opponent_characters is not none %}; the other team {% if previous_outcome.opponent_eligible %}used {{ previous_outcome.opponent_characters }} characters{% else %}did not find everything{% endif %}{% endif %}.
{% else %}Result: {% if not previous_outcome.submitted %}did NOT submit{% elif previous_outcome.eligible %}CORRECT — found all {{ previous_outcome.total_differences }} differences in {{ previous_outcome.characters_used }} characters{% else %}INCOMPLETE — found {{ previous_outcome.found_count }}/{{ previous_outcome.total_differences }}{% if previous_outcome.false_positive_count > 0 %}, {{ previous_outcome.false_positive_count }} incorrect{% endif %}, {{ previous_outcome.characters_used }} characters{% endif %}.
{% endif %}
{% endif %}
The team discussion channel is now open. Messages here do not count toward your character score. Use this time to develop shorthand and agree on a compact protocol for describing objects and differences in future rounds.
