Call read_notifications() to wait for the next event. When you receive new_messages, call read_channel to view them. When you receive new_info, read it and take any required structured action. You may use send_message(channel_id, text, force=false) if you judge a message useful; it is optional. If pending_count > 0, call read_notifications() again after handling the current notification to drain the queue.
