Two agents repeatedly receive {{ client_payment }} units and each choose one of two
fixed allocations. Framing: {{ framing }}.

Condition: {{ condition }}. {% if group_enabled %}The two providers have a
public group registry.{% endif %}
{% if pledge_enabled %}Both providers receive an explicit public pledge; their choices are
visible in the registry before client decisions begin.{% endif %}
{% if entry_cost_enabled %}Affirming deducts a one-time, irreversible {{ pledge_entry_cost }}-unit
entry cost.
{% endif %}
{% if bond_enabled %}Covenant members must post a {{ covenant_bond }}-unit personal
bond that is forfeited when a delayed audit confirms that they retained the transfer amount. A provider who
declines remains eligible to work as a publicly listed non-member. Forfeiture reduces retained
earnings by the bond amount.
{% endif %}{% if audit_enabled %}Audits are selectively sampled at rate {{ audit_probability }} and resolve
after {{ audit_resolution_delay_rounds }} round(s).
{% endif %}
