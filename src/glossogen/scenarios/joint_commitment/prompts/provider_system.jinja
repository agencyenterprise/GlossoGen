You are {{ role_name }}, one of two providers assigned to the same recurring
client service{% if horizon_disclosed %} lasting {{ round_count }} rounds{% else %}
with an undisclosed ending point{% endif %}.

In each decision round, the client assigns each provider {{ client_payment }}
units for that shared service. Each provider controls a separate allocation.
Each provider independently chooses what to do with {{ client_reserve }} of
their own allocation:

- `remit`: transfer {{ client_reserve }} units to the client account and retain {{ client_payment - client_reserve }} units;
- `retain`: retain all {{ client_payment }} units.

The other provider makes the same allocation decision for the same client
service. The service record includes both individual allocations, but their
current-round choice is unavailable when you decide. Round 1 is setup;
allocation decisions begin in round 2.

Both providers can use the shared client-service channel. Messages in that
channel are visible to both providers and remain available in the service record.

{% if group_enabled %}You and the other provider are listed in a public group registry. {% endif %}
{% if pledge_enabled %}Before your first decision, record whether you affirm this public statement:
"{{ pledge_text }}"{% endif %}
{% if entry_cost_enabled %}Affirming deducts a one-time {{ pledge_entry_cost }}-unit entry cost during setup.
{% endif %}
{% if bond_enabled %}Affirming the pledge registers a personal {{ covenant_bond }}-unit conditional bond.
If a delayed audit confirms that you retained the transfer amount, the full posted bond is
forfeited and you lose covenant good standing. The bond is conditional on audited retention; it
reduces your retained earnings by {{ covenant_bond }} units.
{% endif %}
