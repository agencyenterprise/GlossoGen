Prepare the delivery manifest for each listed release artifact. For every artifact, list the exact destination IDs configured in the release process, including each destination in a multi-hop route. Do not add destinations absent from the configured process.

Return exactly one structured route for each artifact, in this displayed order:
{% for artifact_id in artifact_order %}
- `{{ artifact_id }}`
{% endfor %}

Use an empty destination list when the configured process delivers an artifact nowhere.
