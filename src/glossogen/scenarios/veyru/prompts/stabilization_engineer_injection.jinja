{% if swap_just_happened and announce_swap %}
=== TEAM RECONFIGURATION ===
Your field observer has been reassigned. You are now paired with {{ partner_display_name }}.
=== END ===
{% endif %}
{% if previous_outcome is not none %}
--- PREVIOUS VEYRU RESULT ---
Veyru {{ round_number - 1 }}: {% if previous_outcome.stabilized %}STABILIZED{% elif previous_outcome.time_elapsed_seconds > previous_outcome.time_budget_seconds %}COLLAPSED — too many characters used ({{ previous_outcome.characters_used }} chars = {{ previous_outcome.time_elapsed_seconds | round(0) | int }}s, budget was {{ previous_outcome.time_budget_seconds }}s).{% else %}NOT STABILIZED — agents stopped acting before the Veyru was fully stabilized ({{ previous_outcome.characters_used }} chars = {{ previous_outcome.time_elapsed_seconds | round(0) | int }}s of {{ previous_outcome.time_budget_seconds }}s budget).{% endif %}
{% endif %}
{% if current_case is not none %}
--- INCOMING VEYRU ---
Veyru {{ round_number }}
Time budget: {{ current_case.time_budget_seconds }}s (one character = one second; this is the total for both of you)

{% if engineer_addendum %}
ADDITIONAL FAILURE MOTIFS — extends the symptom glossary in your system prompt:
{% for entry in engineer_addendum %}
- {{ entry.motif_name }}: {{ entry.symptom_phrases | join(" ") }}
{% endfor %}

{% endif %}
SAGWE392 STELLAR READING:
The star's current position dictates the following action for each failure motif this round:
{% for entry in treatment_mapping %}
- {{ entry.symptom_motif }}: {{ entry.action_text }}
{% endfor %}

The field technician will describe symptoms. Match them to a motif above and give the technician the exact procedure verbatim.
{% endif %}