CAPACITY WINDOW

The setup record is closed. Use `inspect_capacity_candidates` to reveal every eligible candidate and its capacity cost. Inspection uses no capacity.

Four capacity units are available. Submit the final list of zero, one, or two returned IDs with `submit_capacity_plan`. The displayed costs may total no more than four units. That call records the selection and completes the window.
