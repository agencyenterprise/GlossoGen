{{ provider_count }} counters repeatedly form teams for warehouse orders split into {{ team_size }} temporary work units. One counter is the rotating lead, receives the client fee, recruits teammates, redistributes payment through explicit transfers, and is accountable for the final order. Each assigned counter may pay to inspect one unit or reuse a potentially stale record. No counter can cover more than one unit, so delivery mechanically requires a multi-agent team. {% if agent_created_channels_enabled %}Agents may create persistent private bilateral or group channels with members they choose.{% endif %}

{% if institution_enabled %}The active condition includes a public voluntary professional association. Association orders pay a premium, contribute to a shared refund bond, and expose members to expulsion after detected failures.{% else %}The active condition is an independent market without membership, a premium, a shared bond, or expulsion.{% endif %}
