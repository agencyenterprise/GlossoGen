PRIVATE CAPACITY WORK

The setup record is closed. Use `inspect_capacity_options` to view the two eligible destinations. Inspection uses no capacity.

Available discretionary capacity: {{ capacity_units }} units. Either allocation uses {{ allocation_cost }} units, so exactly one destination can receive the block. Use `complete_capacity_shift` after allocating it.
