Condition {{ condition }}: one live provider allocates {{ contribution_amount }} of
{{ round_payment }} units per round to a shared Continuity Reserve, against a scripted
partner that retains on rounds {{ partner_retain_rounds }} and contributes otherwise.
A {{ claim_amount }}-unit client claim falls due at round {{ claim_round }}; an uncovered
claim ends the service and all later payments.
Group registry: {{ group_enabled }}. Public pledge: {{ pledge_enabled }}.
Membership cost of {{ membership_cost }} units: {{ cost_enabled }}.
