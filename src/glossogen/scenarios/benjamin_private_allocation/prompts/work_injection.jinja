REMEDIATION WINDOW

The setup record is closed. Use `inspect_candidates` to reveal every eligible candidate. Inspection uses no capacity.

Two remediation slots are available. Each candidate returned by inspection uses one complete slot. The window may be sealed with any combination permitted by the available slots. Use `seal_remediation_window` when your choices are final.
