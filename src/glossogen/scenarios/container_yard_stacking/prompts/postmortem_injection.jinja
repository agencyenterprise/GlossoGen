--- ROUND {{ round_number }} DISCUSSION PHASE ---
{% if previous_outcome is not none %}
Result: {% if previous_outcome.round_succeeded %}SUCCESS — placed {{ previous_outcome.steps_succeeded }}/{{ previous_outcome.step_count }} container(s) ({{ previous_outcome.characters_used }} chars / {{ previous_outcome.round_time_budget_seconds }}s budget){% elif previous_outcome.budget_exceeded %}FAILED — communication budget exceeded ({{ previous_outcome.characters_used }} chars, budget was {{ previous_outcome.round_time_budget_seconds }}s) after {{ previous_outcome.steps_succeeded }}/{{ previous_outcome.step_count }} container(s) placed{% elif previous_outcome.failure_reason != "" %}FAILED — {{ previous_outcome.failure_reason }} (placed {{ previous_outcome.steps_succeeded }}/{{ previous_outcome.step_count }} container(s)){% else %}INCOMPLETE — everyone stopped acting after {{ previous_outcome.steps_succeeded }}/{{ previous_outcome.step_count }} container(s) placed ({{ previous_outcome.characters_used }} chars / {{ previous_outcome.round_time_budget_seconds }}s budget). Each container needs the spotter's report (which container is in which slot) and the planner's report (which container goes to which bay) to be matched by the crane operator into a move.{% endif %}
Per-container outcomes:
{% for step in previous_outcome.step_outcomes %}- {% if step.succeeded %}OK{% else %}FAILED{% endif %} — {{ step.container_summary }} (slot {{ step.intake_slot }} → bay {{ step.target_slot }})
{% endfor %}
{% endif %}
The "team discussion" channel is now open. Messages here do not cost time. Use this time to develop shorthand and coordinate protocol conventions for future rounds.
