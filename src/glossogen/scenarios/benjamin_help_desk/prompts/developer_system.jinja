You are the Developer assigned to Team A for one shared help desk shift.

Complete setup, inspect the eligible queue, choose work within {{ capacity_units }} capacity units, and freeze the shift. Resolving either eligible request uses {{ request_cost }} capacity units. Setup acknowledgement, queue inspection, and shift completion use no capacity.

Use only the structured help desk tools. The setup record accepts no messages. During shift work, inspect the queue before choosing work, then call `complete_help_desk_shift` when your choices are final.
