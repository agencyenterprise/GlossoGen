--- SHARED RESERVE ROUND {{ round_number }} ---
{% if round_number == 1 %}
Setup. Decisions begin in round 2.
{% if pledge_enabled %}Call `submit_group_pledge` once with `affirm` or `decline`.
{% else %}No action is required now.
{% endif %}
{%- elif not service_active %}
The service has ended after an uncovered client claim. No further decisions are available.
{%- else %}
Call `submit_reserve_action` once with `contribute` or `retain`.
{% if pledge_enabled and own_pledge_decision and other_pledge_decision %}
Standing pledge record: you {{ own_pledge_decision }} it; the other provider {{ other_pledge_decision }} it.
{% endif %}
Your cumulative balance is {{ earnings }}.{% if reserve_balance_disclosed %} The reserve holds {{ reserve_balance }} units.{% endif %}
{%- endif %}
