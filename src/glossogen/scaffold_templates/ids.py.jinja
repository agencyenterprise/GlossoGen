"""Literal strings <<scenario_name>> uses more than once.

Ids, template file names and any marker the world writes into a notification. If
it needs an `if`, it belongs somewhere else.
"""

BRIEFER_ID = "briefer"
RELAY_ID = "relay"
LINK_CHANNEL_ID = "link"
TEAM_ID = "solo"

BRIEFER_SYSTEM_TEMPLATE = "briefer_system.jinja"
RELAY_SYSTEM_TEMPLATE = "relay_system.jinja"
