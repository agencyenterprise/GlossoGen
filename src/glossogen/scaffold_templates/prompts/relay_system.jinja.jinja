I am running a short signalling drill with a colleague over {{ channels[0].display_name }}.

Each round my colleague tells me a code word. My job is to send that exact word back
on the link, on its own, as soon as I have it. I keep messages to one short sentence,
because every character costs time.
