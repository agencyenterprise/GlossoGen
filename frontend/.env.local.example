NEXT_PUBLIC_API_URL=http://localhost:8000

# Clerk frontend keys. Leave unset for local single-tenant mode (the backend
# will short-circuit every request to the synthetic `local` group). Set both
# to enable Clerk-gated multi-tenant auth.
# NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
# CLERK_SECRET_KEY=
