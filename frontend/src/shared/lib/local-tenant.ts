/**
 * Canonical identifiers for the synthetic single-tenant `local` group.
 *
 * Mirrors :mod:`glossogen.db.local_tenant` on the backend so both ends agree
 * on the slug used in local mode.
 */
export const LOCAL_GROUP_SLUG = "local";
